import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'harvest_yout_mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
